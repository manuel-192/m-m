#!/bin/bash

# Maintainer: manuel (see forum.endeavouros.com)

pkgname=charge-notifier
pkgdesc="Notify when battery is low or high"
url="https://github.com/manuel-192/m-m/raw/master/PKGBUILDs/$pkgname"
pkgver=24
pkgrel=2

arch=('any')
license=('GPL')
depends=()
backup=()

source=(
    $url/battnot
)
sha512sums=('ebb647f4750980a848413ecea563455a4ce30cf4d00157d04f93e61a0f07a71f1f9871a38f2d6493eb19802d6a3f9b532d8ae67fd971d33ee55802ecff339882')

package() {
    local xx name
    for xx in "${source[@]}" ; do
        name=${xx##*/}
        case "$name" in
            *.completion) install -Dm644 $name   $pkgdir/usr/share/bash-completion/completions/${name%.*} ;;
            *.conf)       install -Dm644 $name   $pkgdir/etc/$name ;;
            *)            install -Dm755 $name   $pkgdir/usr/bin/$name ;;
        esac
    done
}
